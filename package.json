{
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate"
  },
  "pnpm": {
    "patchedDependencies": {
      "@hono/node-server": "patches/@hono__node-server.patch",
      "hono": "patches/hono.patch"
    }
  }
}
